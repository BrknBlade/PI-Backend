info:
  name: Show
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/users/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: "2"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
