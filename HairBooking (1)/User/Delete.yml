info:
  name: Delete
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BASE_URL}}/users/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: "2"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
