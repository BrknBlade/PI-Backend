info:
  name: Bookings
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/users/{{id}}/bookings"
  auth: inherit

runtime:
  variables:
    - name: id
      value: "4"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
