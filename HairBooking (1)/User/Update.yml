info:
  name: Update
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{BASE_URL}}/users/{{id}}"
  body:
    type: json
    data: |-
      {
        "name" : "Francisco Franko"
      }
  auth: inherit

runtime:
  variables:
    - name: id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
