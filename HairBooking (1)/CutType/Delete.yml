info:
  name: Delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{BASE_URL}}/cut_types/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
